const blue = require('./blue')
const sadie = require('./sadie')
const sink = require('./sink')

const allCats = [blue, sadie, sink]

module.exports = allCats

//index.js is a starting point of directory to import and export using directory path